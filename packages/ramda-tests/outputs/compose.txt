

  compose
    ✓ is a variadic function
    1) performs right-to-left function composition
    ✓ passes context to functions
    ✓ throws if given no arguments
    ✓ can be applied to one argument

  compose properties
    ✓ composes two functions (66ms)
    ✓ associative (44ms)


  6 passing (192ms)
  1 failing

  1) compose
       performs right-to-left function composition:

      AssertionError [ERR_ASSERTION]: Expected values to be strictly equal:
+ actual - expected

+ 'function f1(a) {\n' +
+   '            if (arguments.length === 0) {\n' +
+   '                return f1;\n' +
+   "            } else if (a != null && a['@@functional/placeholder'] === true) {\n" +
+   '                return f1;\n' +
+   '            } else {\n' +
+   '                return fn.apply(this, arguments);\n' +
+   '            }\n' +
+   '        }'
- '[10, 20, 30]'
      + expected - actual

      -function f1(a) {
      -            if (arguments.length === 0) {
      -                return f1;
      -            } else if (a != null && a['@@functional/placeholder'] === true) {
      -                return f1;
      -            } else {
      -                return fn.apply(this, arguments);
      -            }
      -        }
      +[10, 20, 30]
      
      at strictEqual (test/shared/eq.js:8:10)
      at Context.eq (test/compose.js:20:5)
      at processImmediate (internal/timers.js:439:21)



