import { getComponentInput } from '../_helpers/testInputHolder'
import { editTypings } from './editTypings'

test('', () => {
  const result = editTypings(getComponentInput())
  console.log(result)
})
